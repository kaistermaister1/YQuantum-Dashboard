# Cayman DQI / QAOA stack
#   pip install -r requirements.txt
#
# Windows: if pip reports WinError 32 during guppylang install, pause OneDrive sync on this
# folder or close other Python/IDE processes using .venv, then retry.
# On Windows ARM64, use 64-bit (x86-64) Python from python.org if wasmtime fails to load.
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
guppylang
# guppylang-internals 0.32.x expects hugr<0.16 (.extensions as dict, register_updated).
# qnexus>=0.42 otherwise resolves hugr 0.16+ and kernel.compile() raises AttributeError.
hugr>=0.14,<0.16
# Nexus client for default DQI execution (must resolve with hugr<0.16 above).
qnexus>=0.38,<0.42
# Optional: dqi-main-compatible BP pipeline (local simulation path).
# qiskit
# qiskit-aer
